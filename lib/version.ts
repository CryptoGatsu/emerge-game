/**
 * Which build this is.
 *
 * The version is one constant, read by the line on the landing page and by
 * nothing that has to be correct — it is what a person says to another person
 * ("I'm on 1.3"), not what the update check compares.
 *
 * Bump it when there is something a player would notice. It is not a
 * deployment counter.
 */
export const VERSION = '2.6';
